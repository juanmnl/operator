Second review pass on operator/e78fc0 at bca3c7d. Verified in a scratch worktree: tsc clean, renderer 1052/0 (72 files), electron 498/0 (25 files). cargo not re-run.

VERDICT: fix A and B, then merge. The four first-pass blockers are genuinely fixed — I traced each of the brief's specific challenges against the code, not the account.

RE-VERIFIED 1–9, 11, 16:
- #2 Remote Control on restore: FIXED. One remoteControlLaunch(), both callers, resolves through resolveAgentConfig so custom roles and resumed projects work. Boot restore only builds an offer, so projectsRef is populated by the time any restore runs. Residuals: SessionConfig.remoteControl is now dead (written and read by nothing); a stale saved.projectId silently yields off.
- #3 compacting wedge: FIXED. endCompaction on any real main-thread record + 5-min ceiling; both tailers lockstep. Boundary-then-sidechain is covered by the ceiling; boundary-then-session-end freezes lastPhase but the track is not isAlive so the quit guard never sees it. Residual LOW: compactingSinceMs = Date.now() not the record timestamp, so re-reading an old boundary arms a fresh 5 minutes on every app start.
- #4 the sweep: FIXED. Three gates + OPERATOR_BINARY_RE. Checked each case — a second Operator (Operator.app/electron, and a tauri dev build via the shape gate), docker, ssh, postgres are all refused and surface in the list instead. Residual LOW: two same-named DEV_SERVER_RE (dev-servers.ts:56 vs port-alloc.ts:200), the AUTOMATIC one broader (serve|http-server), which inverts the safety ordering; \bserve\b also matches --mcp-serve, saved only by test ordering.
- #1 provesOwnServer: PARTLY fixed, residual MEDIUM. It does fail closed on every ambiguous case named. But neither surviving gate examines the port: the lease gate is satisfied by construction (the sibling holding the reservation wrote it), and gate 1 is "any depth-≥2 process that isn't claude/mcp-helper/shell" — rg, git, vitest all qualify. One sibling mid-build + a stranger on the port still hands the stranger's server to the next lane. Much narrower, and the split hint no longer asserts "verified free", so not blocking. DEV_SERVER_RE sits 40 lines above, unused by the predicate.
- #6, #7, #8, #9, #16: all FIXED as described. #14 (chat.db write-only) resolved as a side effect — toolOutputStats reads it again.
- #11 v6 fallback: FIXED with a new gap — retryScanWithoutV6 scans windowPorts with NO lease check (scan has one), so on an IPv6-less host it can hand out a port another Operator instance leases, and the result carries no `shared` flag so the lane is told "verified free". Also v6Failures is a cross-scan accumulator compared against a per-scan length, so enough long-lived [::1]-only orphans eventually trip the fallback on a v6-capable host.

NEW:
A — HIGH. SessionToolbar.tsx:116 `tunable = live && phase === 'idle'`. No tracked lane ever has phase 'idle': derivePhase's whole range is running|compacting|waiting and neither tailer contains the string. 'idle' exists only on DashboardView's synthetic placeholder for a tab with no transcript yet. So the tuning chips are interactive for a few seconds after spawn and disabled for the rest of the lane's life, showing "Wait for the lane to finish its turn" on a lane idle for an hour. comms.ts:227 already spells the right test (idle || waiting). Not caught because dev/qa-real-bridge.ts:34,47,51 hardcodes phase:'idle' and drive-tuning-chips.mjs selects the chips by the title that only exists when tunable — a fixture more permissive than reality, same shape as feedback_fixtures_must_match_reality, and the same silent-wrong-enum shape as project_effort_ladder_drift.
B — MEDIUM-HIGH. tuning.ts:130-134 says "latest session wins" but nothing sorts by time; computeTuning returns bySession sorted by TOKENS desc (usage.ts:385), so model/effort/medianContext take the SMALLEST session's values. firstTsMs/lastTsMs are in the payload and unread. The card then names the wrong model and recommends a step down from an effort the lane isn't running — its headline claim, for any lane restarted in the window.
C — MEDIUM. The weak cwd join pass attributes nothing: role comes from `hit` only, so a cwd-only match always keys UNATTRIBUTED, and the first such match labels the "Not attributed" row with an arbitrary project's name. Both the doc comment and the commit message describe the opposite.
D — LOW-MED. getTuning mixes two windows: computeTuning uses a UTC calendar-day cutoff (up to 48h at days=1), toolOutputStats an exact n*24h instant — the exact thing "ONE CALL" exists to prevent.
E — LOW. AgentSession.effort/.compactions captured in BOTH tailers with 9 new tests and read by nothing; the toolbar's effort chip still shows the launch pin while its model chip already falls back to the transcript.
F/G/H — LOW/NIT: project-share bar slices to 8 but divides by the all-project total; a timestamp-less boundary counts in every window; formatCost has two identical branches.

TUNING COMMIT vs the brief's list: tailer lockstep confirmed; percentiles correct (nearest-rank, empty→0, sorted before call, reads outputChars not the capped output); card fires only >25% AND a step available, ranked[0] really is the top (no in-place sort, table not sortable); no forecast anywhere in the copy — strongest claim is "the smallest change that moves this"; plan-meter section confirmed NOT multiplying — weekPct is quoted on its own bar, the split is local tokens/total, with an explicit paragraph on the two denominators and a correct degraded state.

REAL DATA: nothing real is committed and nothing can travel. Checked four ways (all blobs >40KB reachable from the tip; all .json/.jsonl >20KB reachable from ANY ref; git grep for parentUuid/isSidechain/toolUseResult/uuid sessionId; and the one suspicious 43KB test file, which is 43KB on main too). The 431KB dev/qa-real-fixture.json — blob 2916741a, 846 real narration messages plus roster prompts and real paths — is UNREACHABLE from every ref (removed by the amend that made f42e5e5), and the branch has never been pushed to origin. It IS still in .git/objects, held by reflog entry operator/e78fc0@{3} (commit 6584384); `git reflog delete` that entry then `git reflog expire --expire-unreachable=now --all && git gc --prune=now` removes it from the machine. .gitignore now covers dev/*fixture*.json by shape. One caveat: the NEW dev/drive-tuning-chips.mjs:21 reads that fixture at run time and tells the next person to regenerate it with qa-extract-real.mjs, so the chip harness cannot be run by anyone without writing 431KB of private conversation into a public repo again.

Full report at dev/results/review-2-simplify-batch.md, attached as content.
