<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- The standard Electron trio under the hardened runtime, not a widening: V8 JITs, and the
       framework is loaded as an unsigned library from inside the bundle. -->
  <key>com.apple.security.cs.allow-jit</key><true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key><true/>
  <key>com.apple.security.cs.disable-library-validation</key><true/>
  <!-- Operator spawns login shells (ptys) and `claude`; without this the hardened runtime
       refuses to exec a child that is not signed by us, which is every lane. -->
  <key>com.apple.security.cs.allow-dyld-environment-variables</key><true/>
  <key>com.apple.security.cs.debugger</key><false/>
</dict>
</plist>
